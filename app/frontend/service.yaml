apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: kubernetes-app
  labels:
    app: frontend
spec:
  selector:
   app: frontend
  ports:
  - name: web-app
    port: 4000
    targetPort: 4000